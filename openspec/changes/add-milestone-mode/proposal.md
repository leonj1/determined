# Add milestone mode

Add an opt-in bounded outer loop that elaborates, intent-checks, executes, verifies, and when necessary replans independently shippable milestones while preserving flat execution behavior.
