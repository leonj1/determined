# Diff2Html browser assets

`diff2html.min.js` and `diff2html.min.css` are vendored from the published
`diff2html@3.4.55` npm package. The project is distributed under the MIT
license in `LICENSE.diff2html.md`.

Only the core browser bundle is included. It parses and renders unified diffs
without the optional syntax-highlighting UI bundle.
